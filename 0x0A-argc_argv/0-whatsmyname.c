#include "main.h"

/**
 * main - print the programe name
 * @argc: argument count
 * @argv: pointer to artgument vector
 *
 * Return: always(0)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
